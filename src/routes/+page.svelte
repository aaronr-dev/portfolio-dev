<svelte:head><title>Aaron Rodriguez — Senior Data Engineer</title></svelte:head>
<div class="hero">
  <p class="eyebrow">Senior Data Engineer · Huntsville, AL</p>
  <h1>Aaron Rodriguez</h1>
  <p class="desc">Pipelines that don't fail. Reports that aren't stale. 10+ years building cloud-native data infrastructure across Azure, Databricks, and dbt from ingestion to executive-ready output.</p>
  <div class="actions">
    <a href="/experience" class="btn-primary">View Experience</a>
    <a href="/contact" class="btn-ghost">Get in Touch</a>
  </div>
</div>
<div class="cards">
  <div class="card"><div class="card-label">Most Recently</div><div class="card-value">Senior Data Engineer</div><div class="card-sub">National CineMedia · 2022–2026</div></div>
  <div class="card"><div class="card-label">Stack</div><div class="card-value">Azure · Databricks · dbt</div><div class="card-sub">Snowflake · Power BI · Fivetran</div></div>
  <div class="card"><div class="card-label">Service</div><div class="card-value">USMC · OIF II</div><div class="card-sub">Al-Anbar Province · 2004</div></div>
</div>
<div class="about">
  <div class="about-label">About</div>
  <div class="about-body">
    <p>Senior Data Engineer with 10+ years building cloud-native pipelines from ingestion to executive-ready output across Azure and Databricks. Deep experience in medallion architecture, dimensional modeling, and BI delivery across media, adtech, healthcare, real estate, and defense.</p>
    <p>Before data engineering, I served in the United States Marine Corps Reserve as a Field Artillery Cannoneer (0811), deploying to Iraq in 2004 with I MEF in support of Operation Iraqi Freedom II. Previously held Secret clearance. U.S. citizen.</p>
  </div>
</div>
<style>
  .hero { margin-bottom: 2rem; padding-bottom: 2.5rem; border-bottom: 1px solid var(--rule); }
  .eyebrow { font-family: var(--mono); font-size: 0.72rem; color: var(--teal); letter-spacing: 0.06em; margin-bottom: 1rem; }
  h1 { font-size: 2.75rem; font-weight: 600; letter-spacing: -0.03em; color: var(--text); margin-bottom: 1.25rem; line-height: 1.1; }
  .desc { font-size: 1rem; color: var(--muted); max-width: 56ch; line-height: 1.8; margin-bottom: 2rem; }
  .actions { display: flex; gap: 0.75rem; }
  .btn-primary { background: var(--teal); color: #0f172a; font-size: 0.85rem; font-weight: 600; padding: 0.65rem 1.4rem; border-radius: 6px; transition: background 0.15s; }
  .btn-primary:hover { background: var(--teal-dark); color: #fff; }
  .btn-ghost { font-size: 0.85rem; color: var(--muted); padding: 0.65rem 1.4rem; border-radius: 6px; border: 1px solid rgba(255,255,255,0.12); transition: border-color 0.15s, color 0.15s; }
  .btn-ghost:hover { border-color: var(--teal); color: var(--teal); }
  .cards { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; margin-bottom: 2rem; }
  .card { background: var(--bg2); border: 1px solid rgba(255,255,255,0.06); border-top: 2px solid var(--teal); border-radius: 8px; padding: 1.25rem 1.5rem; }
  .card-label { font-family: var(--mono); font-size: 0.62rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--muted); margin-bottom: 0.5rem; }
  .card-value { font-size: 0.9rem; font-weight: 600; color: var(--text); margin-bottom: 0.2rem; }
  .card-sub { font-size: 0.78rem; color: var(--muted); }
  .about { background: var(--bg2); border: 1px solid rgba(255,255,255,0.06); border-radius: 8px; padding: 2rem; display: grid; grid-template-columns: 100px 1fr; gap: 2rem; }
  .about-label { font-family: var(--mono); font-size: 0.7rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--teal); padding-top: 0.15rem; }
  .about-body { display: flex; flex-direction: column; gap: 0.9rem; }
  .about-body p { font-size: 0.9rem; color: var(--muted); line-height: 1.8; }
</style>
